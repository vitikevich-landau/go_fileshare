# Binaries — anchored to the repo root so they do NOT match the cmd/ source
# directories of the same name (that mistake dropped the entry points from Git).
/bin/
*.exe
*.exe~
/fshare-daemon
/fshare-commander

# Test / build artifacts
*.test
*.out
coverage.*

# Runtime data (server)
/data/
*.cache
checksums.cache
users.json
config.json

# Client runtime
profiles.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
